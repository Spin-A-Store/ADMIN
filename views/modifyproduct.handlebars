
        <!-- /. SIDEBAR MENU (navbar-side) -->
        <div id="page-wrapper" class="page-wrapper-cls">
      <div id="page-inner">
                <div class="row">
                    <div class="col-md-12">
                        <h1 class="page-head-line">Product Search &amp; Modify</h1>
                    </div>
                </div>

 <div class="row">
 <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Discription</th>
        <th>category</th>
        <th>status</th>
      </tr>
    </thead>
    <tbody>
    {{#each data.response}}
      <tr>
        <td>{{this.name}}</td>
        {{#with this.desc}}
        <td>{{this.[0].val}}</td>
        {{/with}}
        <td>{{this.category}}</td>
        <td>offline<div style="float:right; font-size: 85%; position: relative; top:-10px"><a id="signinlink" href="/product/modifyproduct?var={{this._id}}" ><span class="fa fa-edit " style="color:black;"></span></a></div></td>
      </tr>
      {{/each}}

    </tbody>
  </table>
                </div>
                </div>
        </div>
        <!-- /. PAGE WRAPPER  -->

 <script src="assets/js/jquery-1.11.1.js"></script>
    <!-- BOOTSTRAP SCRIPTS -->
    <script src="assets/js/bootstrap.js"></script>
    <!-- METISMENU SCRIPTS -->
    <script src="assets/js/jquery.metisMenu.js"></script>
    <!-- CUSTOM SCRIPTS -->

    <script src="assets/js/custom.js"></script>
    <script src="assets/js/dropzone.min.js"></script>
    <script src="assets/js/productBackend.js"></script>
    <script src="assets/js/productDetails.js"></script>
